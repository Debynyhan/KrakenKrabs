# Tunnel configuration for Vite dev server
# Copy to .env.local and adjust values as needed

# Public hostname of your tunnel (e.g., ngrok, Cloudflare Tunnel)
TUNNEL_HOST=3964f012eae9.ngrok-free.app

# Keep HMR enabled by default (set to 1 to disable if you hit RPM limits)
DISABLE_HMR=0
